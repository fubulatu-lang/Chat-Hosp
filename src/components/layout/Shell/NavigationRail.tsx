import { Box } from "@mui/material";

export default function NavigationRail() {
  return (
    <Box
      sx={{
        width: 80,
        borderRight: 1,
        borderColor: "divider"
      }}
    />
  );
}